# Cursor fixtures

`sample/` is generated entirely from the invented messages in
`scripts/create-cursor-fixtures.ts`. Regenerate it with:

```sh
bun scripts/create-cursor-fixtures.ts
```

The committed SQLite file contains no real Cursor session data.
